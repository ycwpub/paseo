{
  "apiVersion": "paseo.sh/workflow/v1",
  "kind": "Workflow",
  "version": 1,
  "name": "字节开发 Meego PRD",
  "description": "从 Meego 需求首页读取当前用户“我参与的”需求，或把指定工作项解析为 PRD。",
  "timeoutMs": 60000,
  "steps": [
    {
      "id": "meego_source",
      "name": "读取 Meego",
      "type": "python",
      "module": "scripts.meego_source",
      "function": "run_node",
      "cwd": "{{workflow.cwd}}/..",
      "inputVariable": "input",
      "outputVariable": "output",
      "retry": { "maxAttempts": 1 },
      "inputSchema": {
        "type": "object",
        "required": ["action"],
        "properties": {
          "action": {
            "type": "string",
            "enum": ["list", "resolve", "login_begin", "login_complete"]
          },
          "homepageUrl": { "type": "string" },
          "url": { "type": "string" },
          "projectKey": { "type": "string" },
          "workItemId": { "type": "string" },
          "completeToken": { "type": "string" }
        },
        "additionalProperties": false
      }
    }
  ]
}
